apiVersion: v1
kind: Service
metadata:
  name: spring-boot-service
  namespace: appdep
spec:
  selector:
    app: spring-boot
  ports:
  - name: http
    port: 5000
    targetPort: 5000
# SQL service YAML


---
apiVersion: v1
kind: Service
metadata:
  name: sql-service
  namespace: appdep
spec:
  selector:
    app: sql
  ports:
  - name: sql
    port: 3306 # Replace with your SQL server port number
    targetPort: 3306 # Replace with your SQL server port number

